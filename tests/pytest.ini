[pytest]
# Pytest configuration for skymap project
# Located in tests/ folder for better organization
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

