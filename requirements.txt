# Core + dev + CASA dependencies for skymap
#
# Core only (from pyproject.toml):
#   pip install -e .
#
# Everything including tests and CASA deconvolution:
#   pip install --extra-index-url https://go.nrao.edu/pypi -e ".[all]"
#
# Or install this file directly (includes CASA extra index below):
#   pip install -r requirements.txt
#   pip install -e .

astropy>=7.0.0
radio-beam>=0.3.6
numpy>=2.0.0
healpy>=1.18.0
matplotlib>=3.10.0
h5py>=3.10.0
scipy>=1.11.0
pytest>=8.0.0

# CASA modular packages for skymap.deconvolve
# - casatasks: deconvolve task (installs casatools automatically)
# - casadata: runtime data tables (required; not auto-installed with casatasks)
# - casacore/python-casacore: NOT needed (bundled in casatools wheels)
--extra-index-url https://go.nrao.edu/pypi
casatasks>=6.6.0
casadata
